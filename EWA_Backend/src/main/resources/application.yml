spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: 'true'
  application:
    name: ewa

  datasource:
    url: jdbc:postgresql://localhost:5432/ewa_db
    username: ewapp
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

jwt:
  secret: aLongSecretKeyForJwtWhichMustBeAtLeast256BitsSoHereIsMyLongKey123!
  expiration: 86400000 # 1 day in ms
